# Dependencias y artefactos de build — se regeneran, no se suben
node_modules/
dist/
apps/desktop/capy.bundle.js
apps/web/capy.bundle.js

# Vendor v1: SÍ se versiona — el repo debe ser autosuficiente (la v1 vive solo en la
# máquina del fundador; el CI y cualquier clon necesitan el vendor para correr los tests).
# Sigue siendo de solo lectura: se regenera con `npm run sync` únicamente desde acá.

# Guiones personales: JAMÁS al repo (regla de privacidad de la casa)
*.fountain
*.fdx

# Basura de SO/editor
Thumbs.db
Desktop.ini
.DS_Store

# Investigación estratégica interna: NO al repo público
docs/empresa/
